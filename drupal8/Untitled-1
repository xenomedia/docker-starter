  `enrollment_id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `bundle_type` mediumtext NOT NULL COMMENT 'vrs_enrollment_master bundle type',
  `aid` varchar(10) NOT NULL DEFAULT '',
  `agency_name` varchar(255) NOT NULL DEFAULT '',
  `agent_number` char(7) NOT NULL DEFAULT '',
  `program_year` int(11) NOT NULL DEFAULT '0',
  `package` varchar(50) NOT NULL DEFAULT '',
  `new_agent_indr` varchar(5) NOT NULL DEFAULT '',
  `tactic_id` int(11) NOT NULL DEFAULT '0',
  `vendor_id` int(11) NOT NULL DEFAULT '0',
  `vendor_class_id` int(11) NOT NULL DEFAULT '0',
  `vendor_prod_id` int(11) NOT NULL DEFAULT '0',
  `product_def_id` int(11) NOT NULL DEFAULT '0',
  `unit_id` int(11) NOT NULL DEFAULT '0',
  `activated` varchar(5) NOT NULL DEFAULT '',
  `activated_date` datetime DEFAULT NULL,
  `active` varchar(5) NOT NULL DEFAULT '',
  `active_date` datetime DEFAULT NULL,
  `firstusedate` datetime DEFAULT NULL,
  `lastusedate` datetime DEFAULT NULL,
  `status` varchar(25) NOT NULL DEFAULT '',
  `cancel_flag` varchar(5) NOT NULL DEFAULT '',
  `createdate` datetime DEFAULT NULL,
  `changedate` datetime DEFAULT NULL,
  `qty` int(11) NOT NULL DEFAULT '0',
  `value` float NOT NULL DEFAULT '0',
  `ytd` float NOT NULL DEFAULT '0',
  `ytd_credits` float NOT NULL DEFAULT '0',
  `carryover` int(11) NOT NULL DEFAULT '0',
  `lvl` int(11) NOT NULL DEFAULT '0',
  `vendorseqno` float NOT NULL DEFAULT '0',
  `seqno` int(11) NOT NULL DEFAULT '0',
  `created` datetime DEFAULT NULL,
  `modified` datetime DEFAULT NULL,
  `order_source` varchar(25) NOT NULL COMMENT 'Order Source',
version: "2"

options:
  verbose: true
syncs:
  SITENAME-sync: # tip: add -sync and you keep consistent names as a convention
    src: '.'
    sync_userid: '82'
    # sync_strategy: 'native_osx' # not needed, this is the default now
    sync_excludes: ['.gitignore', '.git/', '.idea/']
